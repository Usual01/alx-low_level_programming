#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * free_grid - function
 * @grid: MATRIX ROW
 * @height: column
 */
void free_grid(int **grid, int height)
{
	int a;
	int *b;

	for (a = 0; a < height; a++)
	{
		p = grid[i];
		free(p);
	}
	free(p);
}

