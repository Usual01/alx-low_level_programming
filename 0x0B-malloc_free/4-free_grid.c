#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * free_grid - function
 * @grid: MATRIX ROW
 * @height: column
 */
void free_grid(int **grid, int height)
{
	int a;

	for (a = 0; a < height; a++)
	{
		free(grid[i]);
	}
	free(a);
}

